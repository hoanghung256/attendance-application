@model List<fullstack_project.Models.Student>


<div>   
    <h4>Student</h4>
    <hr />
    <dl class="row">
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
    <h3>Display all name in list</h3>
    @foreach (var student in Model)
    {
        <p>@student.Name</p> 
    }

</div>
